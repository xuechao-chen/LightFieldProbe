#include <compatibility.glsl>

uniform sampler2D IndirectIrradiance;
uniform sampler2D IndirectRadiance;

out vec4 result;

void main()
{
	int2 P = int2(gl_FragCoord.xy);
	result = texelFetch(IndirectIrradiance, P, 0) + texelFetch(IndirectRadiance, P, 0);
}