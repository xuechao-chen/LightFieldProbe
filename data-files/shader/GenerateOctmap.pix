#include "../Common/Octahedral.glsl"

uniform samplerCube Cubemap;

out vec3  OctRadiance;

void main()
{
	vec3 Direction = octDecode(gl_FragCoord.xy * 2.0 / textureSize(Cubemap, 0).x - 1.0);

	OctRadiance = texture(Cubemap, Direction, 0).xyz;
}