
in vec4 g3d_Vertex;
in vec3 g3d_Normal;

out vec3 csPosition;

//uniform mat4 ViewMatrix;

void main()
{
	//csPosition = (ViewMatrix * Position).xyz;
	csPosition = (g3d_ObjectToWorldMatrix * g3d_Vertex).xyz;
}